// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_certsignrequest.cpp
//
// Purpose     : Implementation of file adapter_certsignrequest.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-02-07T12:05:52+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_certsignrequest.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace x509
{
namespace csr
{
namespace certsignrequest_binding
{
namespace vsomeip
{

CertSignRequestServiceAdapter::~CertSignRequestServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void CertSignRequestServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::ExportASN1CertSignRequest::method_id:
      HandleCall(service, &ServiceInterface::ExportASN1CertSignRequest, msg);
    break;
    case descriptors::ExportPublicly::method_id:
      HandleCall(service, &ServiceInterface::ExportPublicly, msg);
    break;
    case descriptors::GetConstraints::method_id:
      HandleCall(service, &ServiceInterface::GetConstraints, msg);
    break;
    case descriptors::GetPathLimit::method_id:
      HandleCall(service, &ServiceInterface::GetPathLimit, msg);
    break;
    case descriptors::GetSignature::method_id:
      HandleCall(service, &ServiceInterface::GetSignature, msg);
    break;
    case descriptors::IsCa::method_id:
      HandleCall(service, &ServiceInterface::IsCa, msg);
    break;
    case descriptors::MyProvider::method_id:
      HandleCall(service, &ServiceInterface::MyProvider, msg);
    break;
    case descriptors::SubjectDn::method_id:
      HandleCall(service, &ServiceInterface::SubjectDn, msg);
    break;
    case descriptors::SubjectPubKey::method_id:
      HandleCall(service, &ServiceInterface::SubjectPubKey, msg);
    break;
    case descriptors::Verify::method_id:
      HandleCall(service, &ServiceInterface::Verify, msg);
    break;
    case descriptors::Version::method_id:
      HandleCall(service, &ServiceInterface::Version, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void CertSignRequestServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void CertSignRequestServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void CertSignRequestServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace certsignrequest_binding
} // namespace csr
} // namespace x509
} // namespace ipccom
} // namespace crypto
} // namespace ara


