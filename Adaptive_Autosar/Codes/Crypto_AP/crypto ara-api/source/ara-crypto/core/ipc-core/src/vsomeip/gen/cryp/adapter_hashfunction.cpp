// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_hashfunction.cpp
//
// Purpose     : Implementation of file adapter_hashfunction.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-03-14T12:58:58+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_hashfunction.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace cryp
{
namespace hashfunction
{
namespace hashfunction_binding
{
namespace vsomeip
{

HashFunctionServiceAdapter::~HashFunctionServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void HashFunctionServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::Finish::method_id:
      HandleCall(service, &ServiceInterface::Finish, msg);
    break;
    case descriptors::GetCryptoPrimitiveId::method_id:
      HandleCall(service, &ServiceInterface::GetCryptoPrimitiveId, msg);
    break;
    case descriptors::GetDigest::method_id:
      HandleCall(service, &ServiceInterface::GetDigest, msg);
    break;
    case descriptors::GetDigestService::method_id:
      HandleCall(service, &ServiceInterface::GetDigestService, msg);
    break;
    case descriptors::IsInitialized::method_id:
      HandleCall(service, &ServiceInterface::IsInitialized, msg);
    break;
    case descriptors::StartNoParam::method_id:
      HandleCall(service, &ServiceInterface::StartNoParam, msg);
    break;
    case descriptors::StartReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::StartReadOnlyMem, msg);
    break;
    case descriptors::StartSecretSeed::method_id:
      HandleCall(service, &ServiceInterface::StartSecretSeed, msg);
    break;
    case descriptors::Update::method_id:
      HandleCall(service, &ServiceInterface::Update, msg);
    break;
    case descriptors::UpdateReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::UpdateReadOnlyMem, msg);
    break;
    case descriptors::UpdateRestrictedUseObject::method_id:
      HandleCall(service, &ServiceInterface::UpdateRestrictedUseObject, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void HashFunctionServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void HashFunctionServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void HashFunctionServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace hashfunction_binding
} // namespace hashfunction
} // namespace cryp
} // namespace ipccom
} // namespace crypto
} // namespace ara


