// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_x509dn.cpp
//
// Purpose     : Implementation of file adapter_x509dn.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-02-07T12:05:52+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_x509dn.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace x509
{
namespace x509dn
{
namespace x509dn_binding
{
namespace vsomeip
{

X509DNServiceAdapter::~X509DNServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void X509DNServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::ExportPublicly::method_id:
      HandleCall(service, &ServiceInterface::ExportPublicly, msg);
    break;
    case descriptors::GetAttribute::method_id:
      HandleCall(service, &ServiceInterface::GetAttribute, msg);
    break;
    case descriptors::GetAttributeWithIndex::method_id:
      HandleCall(service, &ServiceInterface::GetAttributeWithIndex, msg);
    break;
    case descriptors::GetDnString::method_id:
      HandleCall(service, &ServiceInterface::GetDnString, msg);
    break;
    case descriptors::MyProvider::method_id:
      HandleCall(service, &ServiceInterface::MyProvider, msg);
    break;
    case descriptors::SetAttribute::method_id:
      HandleCall(service, &ServiceInterface::SetAttribute, msg);
    break;
    case descriptors::SetAttributeWithIndex::method_id:
      HandleCall(service, &ServiceInterface::SetAttributeWithIndex, msg);
    break;
    case descriptors::SetDn::method_id:
      HandleCall(service, &ServiceInterface::SetDn, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void X509DNServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void X509DNServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void X509DNServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace x509dn_binding
} // namespace x509dn
} // namespace x509
} // namespace ipccom
} // namespace crypto
} // namespace ara


