// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_authciphercontext.cpp
//
// Purpose     : Implementation of file adapter_authciphercontext.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-03-14T12:58:58+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_authciphercontext.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace cryp
{
namespace authenticationcipher
{
namespace authciphercontext_binding
{
namespace vsomeip
{

AuthCipherContextServiceAdapter::~AuthCipherContextServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void AuthCipherContextServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::Check::method_id:
      HandleCall(service, &ServiceInterface::Check, msg);
    break;
    case descriptors::GetBlockService::method_id:
      HandleCall(service, &ServiceInterface::GetBlockService, msg);
    break;
    case descriptors::GetCryptoPrimitiveId::method_id:
      HandleCall(service, &ServiceInterface::GetCryptoPrimitiveId, msg);
    break;
    case descriptors::GetDigest::method_id:
      HandleCall(service, &ServiceInterface::GetDigest, msg);
    break;
    case descriptors::GetMaxAssociatedDataSize::method_id:
      HandleCall(service, &ServiceInterface::GetMaxAssociatedDataSize, msg);
    break;
    case descriptors::GetTransformation::method_id:
      HandleCall(service, &ServiceInterface::GetTransformation, msg);
    break;
    case descriptors::IsInitialized::method_id:
      HandleCall(service, &ServiceInterface::IsInitialized, msg);
    break;
    case descriptors::ProcessConfidentialDataReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::ProcessConfidentialDataReadOnlyMem, msg);
    break;
    case descriptors::Reset::method_id:
      HandleCall(service, &ServiceInterface::Reset, msg);
    break;
    case descriptors::SetKey::method_id:
      HandleCall(service, &ServiceInterface::SetKey, msg);
    break;
    case descriptors::StartReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::StartReadOnlyMem, msg);
    break;
    case descriptors::StartSecretSeed::method_id:
      HandleCall(service, &ServiceInterface::StartSecretSeed, msg);
    break;
    case descriptors::UpdateAssociatedData::method_id:
      HandleCall(service, &ServiceInterface::UpdateAssociatedData, msg);
    break;
    case descriptors::UpdateAssociatedDataReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::UpdateAssociatedDataReadOnlyMem, msg);
    break;
    case descriptors::UpdateAssociatedDataResUseObj::method_id:
      HandleCall(service, &ServiceInterface::UpdateAssociatedDataResUseObj, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void AuthCipherContextServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void AuthCipherContextServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void AuthCipherContextServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace authciphercontext_binding
} // namespace authenticationcipher
} // namespace cryp
} // namespace ipccom
} // namespace crypto
} // namespace ara


