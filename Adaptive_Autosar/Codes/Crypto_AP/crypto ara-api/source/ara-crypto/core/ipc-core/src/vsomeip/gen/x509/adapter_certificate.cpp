// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_certificate.cpp
//
// Purpose     : Implementation of file adapter_certificate.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-02-07T12:05:52+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_certificate.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace x509
{
namespace certificate
{
namespace certificate_binding
{
namespace vsomeip
{

CertificateServiceAdapter::~CertificateServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void CertificateServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::AuthorityKeyId::method_id:
      HandleCall(service, &ServiceInterface::AuthorityKeyId, msg);
    break;
    case descriptors::EndTime::method_id:
      HandleCall(service, &ServiceInterface::EndTime, msg);
    break;
    case descriptors::ExportPublicly::method_id:
      HandleCall(service, &ServiceInterface::ExportPublicly, msg);
    break;
    case descriptors::GetConstraints::method_id:
      HandleCall(service, &ServiceInterface::GetConstraints, msg);
    break;
    case descriptors::GetFingerprint::method_id:
      HandleCall(service, &ServiceInterface::GetFingerprint, msg);
    break;
    case descriptors::GetPathLimit::method_id:
      HandleCall(service, &ServiceInterface::GetPathLimit, msg);
    break;
    case descriptors::GetStatus::method_id:
      HandleCall(service, &ServiceInterface::GetStatus, msg);
    break;
    case descriptors::IsCa::method_id:
      HandleCall(service, &ServiceInterface::IsCa, msg);
    break;
    case descriptors::IsRoot::method_id:
      HandleCall(service, &ServiceInterface::IsRoot, msg);
    break;
    case descriptors::IssuerDn::method_id:
      HandleCall(service, &ServiceInterface::IssuerDn, msg);
    break;
    case descriptors::MyProvider::method_id:
      HandleCall(service, &ServiceInterface::MyProvider, msg);
    break;
    case descriptors::SerialNumber::method_id:
      HandleCall(service, &ServiceInterface::SerialNumber, msg);
    break;
    case descriptors::StartTime::method_id:
      HandleCall(service, &ServiceInterface::StartTime, msg);
    break;
    case descriptors::SubjectDn::method_id:
      HandleCall(service, &ServiceInterface::SubjectDn, msg);
    break;
    case descriptors::SubjectKeyId::method_id:
      HandleCall(service, &ServiceInterface::SubjectKeyId, msg);
    break;
    case descriptors::SubjectPubKey::method_id:
      HandleCall(service, &ServiceInterface::SubjectPubKey, msg);
    break;
    case descriptors::VerifyMe::method_id:
      HandleCall(service, &ServiceInterface::VerifyMe, msg);
    break;
    case descriptors::X509Version::method_id:
      HandleCall(service, &ServiceInterface::X509Version, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void CertificateServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void CertificateServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void CertificateServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace certificate_binding
} // namespace certificate
} // namespace x509
} // namespace ipccom
} // namespace crypto
} // namespace ara


