// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_randomgenerator.cpp
//
// Purpose     : Implementation of file adapter_randomgenerator.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-03-14T12:58:58+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_randomgenerator.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace cryp
{
namespace randomgenerator
{
namespace randomgenerator_binding
{
namespace vsomeip
{

RandomGeneratorServiceAdapter::~RandomGeneratorServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void RandomGeneratorServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::AddEntropy::method_id:
      HandleCall(service, &ServiceInterface::AddEntropy, msg);
    break;
    case descriptors::Generate::method_id:
      HandleCall(service, &ServiceInterface::Generate, msg);
    break;
    case descriptors::GetCryptoPrimitiveId::method_id:
      HandleCall(service, &ServiceInterface::GetCryptoPrimitiveId, msg);
    break;
    case descriptors::GetExtensionService::method_id:
      HandleCall(service, &ServiceInterface::GetExtensionService, msg);
    break;
    case descriptors::IsInitialized::method_id:
      HandleCall(service, &ServiceInterface::IsInitialized, msg);
    break;
    case descriptors::SeedReadOnlyMem::method_id:
      HandleCall(service, &ServiceInterface::SeedReadOnlyMem, msg);
    break;
    case descriptors::SeedSecretSeed::method_id:
      HandleCall(service, &ServiceInterface::SeedSecretSeed, msg);
    break;
    case descriptors::SetKey::method_id:
      HandleCall(service, &ServiceInterface::SetKey, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void RandomGeneratorServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void RandomGeneratorServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void RandomGeneratorServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace randomgenerator_binding
} // namespace randomgenerator
} // namespace cryp
} // namespace ipccom
} // namespace crypto
} // namespace ara


