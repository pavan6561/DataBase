// -----------------------------------------------------------------------------------------------------------
// Copyright (C) AVIN SYSTEMS PVT LTD 2021 - All Rights Reserved Reproduction, distribution and utilization
// of this document as well as the communication of its contents to others without explicit authorization is
// prohibited. Offenders will be held liable for the payment of damages.
// -----------------------------------------------------------------------------------------------------------
// File Name   : adapter_iointerface.cpp
//
// Purpose     : Implementation of file adapter_iointerface.cpp
// -----------------------------------------------------------------------------------------------------------
// CODE GENERATED BY        : CM Code Generator
//
// CODE GENERATOR VERSION   : 1.4.0.A-006
//
// GENERATED ON             : 2022-02-07T12:05:52+05:30
// -----------------------------------------------------------------------------------------------------------

#include "adapter_iointerface.h"
namespace ara
{
namespace crypto
{
namespace ipccom
{
namespace common
{
namespace iointerface
{
namespace iointerface_binding
{
namespace vsomeip
{

IOInterfaceServiceAdapter::~IOInterfaceServiceAdapter() noexcept {
  StopOfferService(ServiceDescriptor::service_id, GetInstanceId());
  UnregisterMethodDispatcher(ServiceDescriptor::service_id);
  Disconnect(dynamic_cast<ServiceInterface&>(service_));
}

void IOInterfaceServiceAdapter::DispatchMethodCall(const std::shared_ptr<::vsomeip::message>& msg)
{
  ServiceInterface& service{dynamic_cast<ServiceInterface&>(service_)};
  switch(msg->get_method()) {
    case descriptors::GetAllowedUsage::method_id:
      HandleCall(service, &ServiceInterface::GetAllowedUsage, msg);
    break;
    case descriptors::GetCapacity::method_id:
      HandleCall(service, &ServiceInterface::GetCapacity, msg);
    break;
    case descriptors::GetCryptoObjectType::method_id:
      HandleCall(service, &ServiceInterface::GetCryptoObjectType, msg);
    break;
    case descriptors::GetObjectId::method_id:
      HandleCall(service, &ServiceInterface::GetObjectId, msg);
    break;
    case descriptors::GetPayloadSize::method_id:
      HandleCall(service, &ServiceInterface::GetPayloadSize, msg);
    break;
    case descriptors::GetPrimitiveId::method_id:
      HandleCall(service, &ServiceInterface::GetPrimitiveId, msg);
    break;
    case descriptors::GetTypeRestriction::method_id:
      HandleCall(service, &ServiceInterface::GetTypeRestriction, msg);
    break;
    case descriptors::IsObjectExportable::method_id:
      HandleCall(service, &ServiceInterface::IsObjectExportable, msg);
    break;
    case descriptors::IsObjectSession::method_id:
      HandleCall(service, &ServiceInterface::IsObjectSession, msg);
    break;
    case descriptors::IsValid::method_id:
      HandleCall(service, &ServiceInterface::IsValid, msg);
    break;
    case descriptors::IsVolatile::method_id:
      HandleCall(service, &ServiceInterface::IsVolatile, msg);
    break;
    case descriptors::IsWritable::method_id:
      HandleCall(service, &ServiceInterface::IsWritable, msg);
    break;
    default:
    // Avoid Warning.
    break;
  }
}

void IOInterfaceServiceAdapter::Connect(ServiceInterface& service) {
  service.AddDelegate(*this);
}

void IOInterfaceServiceAdapter::Disconnect(ServiceInterface& service) {
  service.RemoveDelegate(*this);
}

void IOInterfaceServiceAdapter::SendInitialNotification(ServiceInterface& service) {
}

} // namespace vsomeip
} // namespace iointerface_binding
} // namespace iointerface
} // namespace common
} // namespace ipccom
} // namespace crypto
} // namespace ara


